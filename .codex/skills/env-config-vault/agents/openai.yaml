interface:
  display_name: "环境配置库"
  short_description: "管理跨项目复用的环境变量、API Key 和数据库连接配置"
  default_prompt: "Use $env-config-vault to inspect this new project's required environment variables and prepare a safe local .env file from my saved config vault."

policy:
  allow_implicit_invocation: false
